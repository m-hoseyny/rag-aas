# OpenAI Configuration
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1  # Or your custom endpoint

# Vector Database Configuration
VECTOR_DB_USERNAME=postgres
VECTOR_DB_PASSWORD=postgres
VECTOR_DB_HOSTNAME=postgres  # Use 'postgres' for docker-compose, 'localhost' for local development
VECTOR_DB_DATABASE=ragaas
VECTOR_DB_PORT=5432

# Application Settings
DEBUG=True  # Set to False in production
HOST=0.0.0.0
PORT=8443

# LLM Settings
DEFAULT_MODEL=gpt-4  # Default LLM model to use
EMBEDDING_MODEL=text-embedding-3-large  # Model for generating embeddings
TEMPERATURE=0.5  # Default temperature for LLM responses
SIMILARITY_THRESHOLD=0.4  # Threshold for vector similarity search